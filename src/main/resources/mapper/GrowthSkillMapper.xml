<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.growthlens.mapper.GrowthSkillMapper">

    <resultMap id="BaseResultMap" type="com.example.growthlens.entity.GrowthSkill">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="skill_name" property="skillName"/>
        <result column="skill_type" property="skillType"/>
        <result column="master_level" property="masterLevel"/>
        <result column="master_date" property="masterDate"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.growthlens.entity.GrowthSkill" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO growth_skill (
            user_id, skill_name, skill_type, master_level, master_date, remark, create_time, update_time
        ) VALUES (
            #{userId}, #{skillName}, #{skillType}, #{masterLevel}, #{masterDate}, #{remark}, NOW(), NOW()
        )
    </insert>

    <update id="update" parameterType="com.example.growthlens.entity.GrowthSkill">
        UPDATE growth_skill SET
            skill_name = #{skillName},
            skill_type = #{skillType},
            master_level = #{masterLevel},
            master_date = #{masterDate},
            remark = #{remark},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM growth_skill WHERE id = #{id}
    </delete>

    <select id="countBySkillType" resultType="java.util.Map">
        SELECT skill_type AS name, COUNT(*) AS value
        FROM growth_skill
        WHERE user_id = #{userId}
        GROUP BY skill_type
    </select>

    <select id="countByMasterLevel" resultType="java.util.Map">
        SELECT master_level AS name, COUNT(*) AS value
        FROM growth_skill
        WHERE user_id = #{userId}
        GROUP BY master_level
        ORDER BY master_level
    </select>

</mapper>

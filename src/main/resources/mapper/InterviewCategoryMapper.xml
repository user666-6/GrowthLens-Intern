<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.growthlens.mapper.InterviewCategoryMapper">

    <resultMap id="BaseResultMap" type="com.example.growthlens.entity.InterviewCategory">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="category_name" property="categoryName"/>
        <result column="category_type" property="categoryType"/>
        <result column="parent_id" property="parentId"/>
        <result column="sort" property="sort"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.growthlens.entity.InterviewCategory" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO interview_category (
            user_id, category_name, category_type, parent_id, sort, remark, create_time, update_time
        ) VALUES (
            #{userId}, #{categoryName}, #{categoryType}, #{parentId}, #{sort}, #{remark}, NOW(), NOW()
        )
    </insert>

    <update id="update" parameterType="com.example.growthlens.entity.InterviewCategory">
        UPDATE interview_category SET
            category_name = #{categoryName},
            category_type = #{categoryType},
            parent_id = #{parentId},
            sort = #{sort},
            remark = #{remark},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM interview_category WHERE id = #{id}
    </delete>

    <select id="findById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM interview_category WHERE id = #{id}
    </select>

    <select id="findByUserId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT * FROM interview_category WHERE user_id = #{userId} ORDER BY sort ASC, create_time DESC
    </select>

    <select id="findByType" resultMap="BaseResultMap">
        SELECT * FROM interview_category WHERE user_id = #{userId} AND category_type = #{categoryType} ORDER BY sort ASC
    </select>

    <select id="findByParentId" resultMap="BaseResultMap">
        SELECT * FROM interview_category WHERE user_id = #{userId} AND parent_id = #{parentId} ORDER BY sort ASC
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM interview_category ORDER BY create_time DESC
    </select>

</mapper>